# Docker Environment Configuration for Frontend Team
# Chỉ cần thiết lập RESEND_API_KEY nếu muốn test email features

# Email Service (Optional - chỉ cần nếu test email)
# Get your API key from https://resend.com/api-keys
# RESEND_API_KEY=re_your_resend_api_key_here

# ⚠️ Các config khác đã được set mặc định trong docker-compose.yml
# Không cần thay đổi gì thêm!
